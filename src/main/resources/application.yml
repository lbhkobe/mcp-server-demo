server:
  port: 8082

spring:
  application:
    name: mcp-server-demo
  ai:
    mcp:
      server:
        name: mcp-server-demo
        version: 1.0.0
        type: ASYNC # Recommended for reactive applications
        instructions: "这个mcp server可以提供用户查询MSC商品信息"
        sse-message-endpoint: /mcp/queryProduct
        capabilities:
          tool: true
          resource: true
          prompt: true
          completion: true

